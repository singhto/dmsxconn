import 'package:flutter/material.dart';

class DmsxLoadList extends StatelessWidget {
  const DmsxLoadList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('โหลดข้อมูล'),),body: Text('data'),);
  }
}